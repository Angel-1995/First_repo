/* DROP removes the entire record plus the table */

/* DELETE removes entry one by one */ --- /* auto-increment values are not reset when used that is, if u input a new entry and the table had 10 entries before, the new entry is number 11 */

/* TRUNCATE removes all entries but not the table structure */ /* auto-increment values are reset when used */